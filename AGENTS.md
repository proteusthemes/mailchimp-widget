See DEVELOPMENT.md for build and verification commands.

Preserve the release package allowlist and output directory expected by `bin/deploy-plugin.sh`. Keep build commands local; never invoke the deployment script during tests. Update `readme.txt` and regenerate `readme.md` when changing plugin documentation.
